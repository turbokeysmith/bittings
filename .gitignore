# Secrets — NEVER commit these
.env
.env.*
*.key
*.pem

# Dependencies / build
node_modules/
.netlify/
dist/
build/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Deploy cache (Cloudflare Wrangler) — regenerated on each deploy
.wrangler/

# Regenerable demo bundle (re-zip bittings-app/ when needed)
bittings-app.zip
